spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/budgeting_dev
    username: your_username
    password: your_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true